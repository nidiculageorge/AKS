type: AZURE
config:
  storage_account: "kubecostne"
  storage_account_key: "*****"
  container: "kubecost"
  # config.endpoint is only needed if primary blob service endpoint domain is not blob.core.windows.net
  # Example: blob.core.chinacloudapi.cn
  max_retries: 0
